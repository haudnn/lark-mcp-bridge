{
  "name": "lark-mcp-bridge",
  "version": "1.0.0",
  "description": "Streamable-HTTP bridge that wraps @larksuiteoapi/lark-mcp for agent access",
  "main": "dist/index.js",
  "scripts": {
    "dev": "ts-node src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@larksuiteoapi/lark-mcp": "0.5.0",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.5"
  },
  "engines": {
    "node": ">=20"
  }
}
